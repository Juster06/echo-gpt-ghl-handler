envVars:
  - key: NODE_VERSION
    value: 18.17.1

services:
  - type: web
    name: echo-gpt-ghl-handler
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
